"""rolescout.telemetry — local-first telemetry (product-spec §7).

Default: zero data leaves the machine. Store lives at ~/.rolescout/telemetry.db
(ROLESCOUT_HOME override), never inside the repo.
"""
