"""rolenavi.web — LOCAL web UI (127.0.0.1 only, stdlib http.server pattern like
app/server.py). Not a hosted service: no auth, no tenancy, binds loopback only —
product-spec §10's non-goal is hosted multi-tenant SaaS, not a local UI."""
